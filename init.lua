-- bootstrap lazy.nvim, LazyVim and your plugins
require("config.lazy")

-- if vim.fn.has("unix") == 1 and vim.fn.executable("gsettings") == 1 and not vim.g.vscode then
--   require("script.auto_colorscheme").set_color_scheme_from_system()
-- end
